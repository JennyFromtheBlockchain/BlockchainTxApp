database: mysql -u root
create table bch_network ( blockchainTicker varchar(4), blockNumber int, transactions int, timestamp bigint, primary key (blockNumber));
create table xrp_network ( blockchainTicker varchar(4), transactions int, timestamp bigint, txInterval varchar(10), primary key (timestamp, txInterval));
create table snt_network ( blockchainTicker varchar(5), txInterval varchar(10), transactions int, timestamp bigint, primary key (timestamp, txInterval));
create table blockchains ( ticker varchar(5) not null, name varchar(30) not null, apiUrl varchar(255) not null, blockHeightApiUrl varchar(255), primary key (ticker));
